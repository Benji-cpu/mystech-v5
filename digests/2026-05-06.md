# MysTech digest — 2026-05-06

## Headline

- Status: **BLOCKED — cron endpoint unreachable (403)**
- New feedback in last 24h: unknown
- Total pending: unknown
- Health anomalies: unknown (run did not complete)

## What happened

The agent called `GET /api/cron/nightly-routine?digest=true` with the correct `Authorization: Bearer` header.
Vercel returned **HTTP 403** with body `Host not in allowlist` before the request reached the route handler.

This is a **Vercel platform-level IP allowlist** rejecting the Claude Code agent sandbox's egress IP.
The app's own auth logic (which returns 401 for a bad secret) was never invoked.

## Impact

- Nightly Resend digest email was **not sent** to ADMIN_EMAIL today.
- Feedback digest, health checks (stuck readings, failed AI generations, idle decks), and DB queries **did not run**.
- No data was written or changed.

## Action required (human)

1. **Short term**: Log into the Vercel dashboard → Project Settings → Security → IP Allowlist.  
   Either remove the allowlist (if it's not intentional), or add the Claude Code agent's egress IP range.

2. **Alternative**: Route the cron call through a fixed-IP proxy (e.g., a Vercel Edge Config secret + a Vercel function that relays internally, or a fly.io micro-service with a static IP).

3. **Verify**: After unblocking, re-run the agent manually or wait for the next scheduled trigger at 03:22 WITA.

## Errors during run

- `403 Host not in allowlist` from `https://mystech-v5.vercel.app/api/cron/nightly-routine?digest=true`

## Notes

- The middleware (`src/middleware.ts`) does **not** include `/api/cron/*` in its matcher, confirming the 403 originates from Vercel infra, not app code.
- No DB was queried; no Resend call was made. Safe to re-trigger once the IP issue is resolved.
