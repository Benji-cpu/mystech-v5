# MysTech digest — 2026-05-05

## Headline
- New feedback in last 24h: unknown (endpoint blocked)
- Total pending: unknown
- Health anomalies: endpoint returned HTTP 403 — run did not complete

## Blocked run

The nightly-routine endpoint returned **HTTP 403 Forbidden** when called with the provided CRON_SECRET.

Possible causes:
- CRON_SECRET value has rotated or does not match what the Vercel environment has set
- The route has an additional auth layer (e.g. Vercel protection, IP allowlist, or middleware change) that rejected the request before the Bearer check ran
- The route path has changed

No feedback digest email was sent via Resend. No database operations were performed. No health checks were run.

## Suggested follow-ups
- Verify `CRON_SECRET` in Vercel project settings matches the value registered with the Claude Code remote agent at claude.ai/code/scheduled
- Check Vercel deployment logs for the 403 source (middleware vs. route handler)
- Re-run the agent once the secret is confirmed

## Errors during run
- HTTP 403 from `GET https://mystech-v5.vercel.app/api/cron/nightly-routine?digest=true`
