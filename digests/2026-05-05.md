# MysTech digest — 2026-05-05

## Headline
- New feedback in last 24h: unknown (endpoint blocked)
- Total pending: unknown
- Health anomalies: endpoint blocked by sandbox egress — run did not complete

## Blocked run

The nightly-routine endpoint returned **HTTP 403 Forbidden** with body `Host not in allowlist`.

This is a **sandbox egress block**: the Claude Code remote agent's outbound network cannot reach `mystech-v5.vercel.app`. The request was rejected at the network layer before it arrived at Vercel. This is not a CRON_SECRET mismatch.

No feedback digest email was sent via Resend. No database operations were performed. No health checks were run.

## Suggested follow-ups
- The CRON_SECRET value itself is not the issue — the agent's sandbox blocks outbound egress to this host.
- Check whether the Claude Code remote agent environment has a network allowlist that needs updating to permit `mystech-v5.vercel.app`.
- As a workaround, the nightly route can be triggered directly (e.g. via a GitHub Actions cron that has unrestricted egress) and the agent can be limited to summarising a pre-fetched payload.

## Errors during run
- HTTP 403 from `GET https://mystech-v5.vercel.app/api/cron/nightly-routine?digest=true` — response body: `Host not in allowlist`
